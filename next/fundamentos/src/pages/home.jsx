import Layout from '../components/Layout';

const home = (props) => {
    return (
        <Layout title="Início">
            Início
        </Layout>
    )
}

export default home;
// as props passadas são somente para leitura